# Cavaleiro 4 — o segredo do jeito ingenuo: um Secret nativo do Kubernetes.
# Lembre-se: Secret do Kubernetes NAO e criptografia. O valor fica apenas em
# base64 no etcd, e qualquer pessoa com permissao de leitura ve em texto puro.
# (stringData deixa o valor em claro AQUI no manifest — ou seja, no git tambem.)
apiVersion: v1
kind: Secret
metadata:
  name: payments-api
  namespace: c4-secrets
type: Opaque
stringData:
  api-key: "sk-live-51H8xExAmPlE0nAoUseChaveRealAquiJamais"
