# Cavaleiro 4 — a senha do BANCO (a mesma que o backend do Cavaleiro 3 usa para
# falar com o payments-db), guardada do jeito ingenuo: um Secret do Kubernetes.
# Lembre: Secret NAO e criptografia. O valor fica so em base64 no etcd, e
# qualquer um com permissao de leitura ve em texto puro. (stringData deixa o
# valor em claro AQUI no manifest — ou seja, no git tambem.)
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: c4-secrets
type: Opaque
stringData:
  password: "S3nha-d0-Banc0-em-texto-puro-no-git"
